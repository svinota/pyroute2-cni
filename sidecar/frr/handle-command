#!/usr/bin/env bash
set -euo pipefail

FRR_CONF=/etc/frr/frr.conf

read -r cmd
case "$cmd" in
  reload)
    vtysh -b "$FRR_CONF"
    echo OK
    ;;
  restart)
    /usr/lib/frr/watchfrr.sh restart bgpd
    echo OK
    ;;
  *)
    echo ERR unknown command
    ;;
esac
