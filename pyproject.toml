[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[[tool.mypy.overrides]]
module = ["pyroute2.*", "pytest.*", "pytest_asyncio.*"]
ignore_missing_imports = true
